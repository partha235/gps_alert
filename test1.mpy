from machine import Pin
from utime import sleep_ms
import sys

s=Pin(14,Pin.IN)
while True:
    print(s.value())
    if s.value():
        print('sensor detected')
        sys.exit()
    # sleep_ms(10)
